# SmartOps OpenEnv (Upgraded)

AI environment for customer support triage using LLM + semantic scoring.

## Features
- Real-world support workflow
- Semantic reward using embeddings
- Multi-task difficulty

## Run
docker build -t smartops .
docker run smartops
